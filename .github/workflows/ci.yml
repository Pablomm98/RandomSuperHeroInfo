name: Android CI

on:
  push:
    branches: [ "ci/cd", "room", "navigation3", "flows", "unitTests", "ciTesting", "master" ]
  pull_request:
    branches: [ "ci/cd", "room", "navigation3", "flows", "unitTests", "ciTesting", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Setup JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: gradle
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
      - name: Build app with Gradle
        run: ./gradlew assemble
